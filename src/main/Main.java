package main;

import basic.ArraySelectionSort;
import multithreading.*;

import java.io.File;
import java.util.TimerTask;
import java.util.Timer;
import java.util.concurrent.ExecutionException;

public class Main {
    public static void main(String[] args) throws ExecutionException, InterruptedException {
        //CreateThread_1.launch();
        //ThreadLifeCycle.launch();
        //Sinhronization.launch();
        //VolatileClass.launch();
        //AtomicVariables.launch();
        //Deadlock.launch();
        // WaitAndNotify.launch();
        //TryLockClass.launch();

        /*try {
            ExecutorsClass.launch();
        } catch (ExecutionException e) {
            e.printStackTrace();
        } catch (InterruptedException e) {
            e.printStackTrace();
        }*/

        //ScheduledExcecuterService.launch();

        //SemaforsClass.launch();
        //CountDownLatchClass.launch();
        //ExchengerClass.launch();
        //CyclicBarierClass.launch();
        //BlockingQueueClass.launch();
        //ThreadFactoryClass.launch();
        //ForkJoinFrameworkClass.launch();

        //ArraySelectionSort.start();

        /*int a = 6;
        int g = 2;
        a = a+g-(g=a);
        System.out.println("a " + a);
        System.out.println("g " + g);*/

        // Runnable runnable = () -> System.out.println("runnable");;
        // new Thread(runnable).start();
        
    }


}








